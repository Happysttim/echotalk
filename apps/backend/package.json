{
  "name": "@apps/backend",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build -o dist/server ./cmd/server",
    "test": "go test ./...",
    "clean": "rm -rf dist"
  }
}